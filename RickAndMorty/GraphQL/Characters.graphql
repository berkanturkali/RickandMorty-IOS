query Characters(
    $page: Int!
) {
    characters(page: $page) {
        results {
            id
            name
            status
            gender
            species
            image
            episode {
                id
            }
            origin {
                name
            }
            location {
                name
            }
        }
    }
}
